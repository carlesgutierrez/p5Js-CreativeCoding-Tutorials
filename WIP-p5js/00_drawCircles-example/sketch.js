 let sizeCircle = 60;//"asdasd"

function setup() {
  createCanvas(600, 600); //window.screen.width, window.screen.height


}

function draw() {
  	background(0);
	ellipse(mouseX, mouseY, sizeCircle, sizeCircle);
}